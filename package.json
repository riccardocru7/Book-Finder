{
    "name": "book-finder",
    "version": "1.0.0",
    "description": "Book Finder è un'applicazione web che consente agli utenti di scoprire libri in base ai propri gusti letterari.",
    "scripts": {
        "clean": "rm -rf .parcel-cache dist",
        "start": "parcel src/index.html",
        "build": "parcel build src/index.html",
        "deploy": "npm run build && gh-pages -d dist"
    },
    "dependencies": {
        "axios": "^1.7.7",
        "lodash": "^4.17.21"
    },
    "devDependencies": {
        "buffer": "^6.0.3",
        "gh-pages": "^6.2.0",
        "parcel": "^2.10.0",
        "process": "^0.11.10"
    }
}
